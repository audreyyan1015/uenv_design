id: datasets/swe-lite
version: 1.0.0
entrypoints:
  dataset_adapter: swe_lite.dataset_adapter:SweLiteAdapter
  environment: swe_lite.environment:SweLiteEnvironment
  scorer: swe_lite.scorer:SweLiteScorer
models:
  input: swe_lite.models:SweLiteInput
  private_data: swe_lite.models:SweLitePrivateData
internet_access: false
required_capabilities:
- exec.v1
- filesystem.v1
