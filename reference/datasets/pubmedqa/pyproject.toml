[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[project]
name = "uenv-dataset-pubmedqa"
version = "1.0.0"
requires-python = ">=3.11"
dependencies = ["uenv-sdk-reference==0.1.0", "uenv-reference-rules==0.1.0"]

[tool.setuptools.packages.find]
where = ["src"]

